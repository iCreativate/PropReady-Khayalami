[build]
  command = "npm run build"
  # Explicitly set to empty to override UI setting - plugin will handle it
  publish = ""

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

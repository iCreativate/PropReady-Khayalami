[build]
  command = "npm run build"
  # Publish directory is handled by @netlify/plugin-nextjs
  # Do not set publish in Netlify UI - it will conflict with the plugin

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

/**
 * Public buyer Properties browse (`/search`).
 * Keep off until agents publish live listings for buyers to discover.
 * Agent listing tools at `/agents/properties` stay available regardless.
 */
export const PUBLIC_PROPERTIES_ENABLED = false;
